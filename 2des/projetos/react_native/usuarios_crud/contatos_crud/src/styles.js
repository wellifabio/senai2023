import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        backgroundColor: '#ca2',
    },
    tabBar: {
        position: 'absolute',
        backgroundColor: '#fff',
        borderTopWidth: 0,
        bottom: 14,
        left: 14,
        right: 14,
        elevation: 0,
        borderRadius: 4,
        height: 60,
    },
});

export default styles;